extends game_include

block content
  canvas#bg
  canvas#unit
  canvas#ui
  canvas#tooltip
  canvas#minimap
  canvas#menu
  canvas#left_menu
  canvas#right_menu
  script(type='text/javascript').
    console.log(!{JSON.stringify(user)}, !{JSON.stringify(hero)}, !{JSON.stringify(map)});
    Game.getInstance(!{JSON.stringify(user)}, !{JSON.stringify(hero)}, !{JSON.stringify(map)});